# 苍穹外卖测试 — 人员分工

---

## 项目概况

| 项目 | 说明 |
|---|---|
| 名称 | 苍穹外卖（Sky Take-Out） |
| 仓库 | https://github.com/Sonder-MX/sky-take-out.git |
| 架构 | Spring Boot 3.1.2 + MyBatis + MySQL + Redis |
| 模块 | sky-common / sky-pojo / sky-server |
| 测试工具 | JUnit5 + Mockito + H2 + JMeter + Postman |
| 项目管理 | Jira |
| 现有测试 | 零，需从零搭建 |

---

## 每人分工总表

| 编号 | 姓名 | 负责模块 | 核心接口 | 预估用例 |
|---|---|---|---|---|
| M1 | ___ | 组长：环境 + Jira + CI + 集成/系统测试 + 报告整合 | 全部 | — |
| M2 | ___ | 员工管理 + JWT 认证 | EmployeeController | 25-35 |
| M3 | ___ | 分类管理 + 菜品管理 | CategoryController / DishController | 40-55 |
| M4 | ___ | 套餐管理 + 文件上传 | SetmealController / CommonController | 20-30 |
| M5 | ___ | 管理端订单 + 定时任务 | OrderController(admin) / OrderTask | 30-40 |
| M6 | ___ | C端：用户 + 地址簿 | UserController / AddressBookController | 25-35 |
| M7 | ___ | C端：购物车 + 下单 + 支付 | ShoppingCartController / OrderController(user) | 50-70 |
| M8 | ___ | 报表 + 基础设施 + 性能 + 安全 | ReportController / JMeter / 安全测试 | 25-35 |

---

## 各成员详细任务

> 每个成员的任务按 **单元测试** → **集成测试** → **系统测试** 三个阶段拆解

---

### M1 — 组长

**角色定位**：搭建测试基础设施，管理 Jira，协调三个阶段测试，整合最终报告

#### 单元测试阶段（W1-W2）
- [ ] `pom.xml` 添加 JUnit5、Mockito、H2、spring-boot-starter-test 依赖
- [ ] 创建 Jira 项目，建 4 个 Epic、配看板，给 7 人创建全部 Task
- [ ] 每天 Review 至少 1 人的单元测试代码
- [ ] 搭建 GitHub Actions CI：Push 自动跑全量单元测试

#### 集成测试阶段（W3）
- [ ] 用 Docker 启动 MySQL + Redis 共享测试环境，IP 发群里
- [ ] 编写 Postman Collection，覆盖全部 API 端点
- [ ] 编排集成测试执行顺序，协调各模块联调

#### 系统测试阶段（W4-W5）
- [ ] 执行 Postman 全量 API 自动化测试
- [ ] 接收 M8 性能测试数据，撰写报告第 3.2 节和第 6 章
- [ ] 从 Jira 导出缺陷统计，撰写报告第 7 章
- [ ] 收集全员素材，整合第 1、2、5 章
- [ ] W5 整合全部章节成 Word 终稿，检查格式，提交

---

### M2 — 员工管理 + JWT 认证

**负责文件**：`EmployeeController.java`、`EmployeeServiceImpl.java`、`EmployeeMapper.java/xml`、`JwtTokenAdminInterceptor.java`

#### 单元测试阶段（W2）
- [ ] Controller 层：用 MockMvc 测 7 个接口，Mock Service，验证 HTTP 状态码和响应 JSON
- [ ] Service 层：Mock Mapper，测 login 的 3 条异常分支（用户名不存在/密码错误/账号锁定）
- [ ] Mapper 层：@MybatisTest + H2，验证 SQL 正确性（唯一约束、分页）
- [ ] 用等价类 + 边界值分别设计 login 用例，W3 做方法对比

#### 集成测试阶段（W3）
- [ ] 场景：登录 → 拿到 Token → 带 Token 调分页/新增/修改接口 → 验证 JWT 鉴权全链路
- [ ] 场景：错误密码登录 → 验证 GlobalExceptionHandler 正确返回错误 JSON

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化，确认自己的 7 个接口全部通过
- [ ] 配合 M8 性能测试，提供 Employee 模块接口说明
- [ ] 整理自己模块的 Bug 列表，写个人总结

---

### M3 — 分类管理 + 菜品管理

**负责文件**：`CategoryController.java`、`DishController.java`、`CategoryServiceImpl.java`、`DishServiceImpl.java`、`CategoryMapper.java`、`DishMapper.java`、`DishFlavorMapper.java`

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测分类 CRUD（5 个接口）+ 菜品 CRUD（5 个接口）
- [ ] Service 层：Mock Mapper，重点测删除分类时菜品关联校验 → DeletionNotAllowedException
- [ ] Mapper 层：@MybatisTest + H2，验证关联查询 SQL、动态条件分页
- [ ] 用场景法 + 因果图分别设计用例，W3 做方法对比

#### 集成测试阶段（W3）
- [ ] 场景：创建分类 → 创建菜品关联该分类 → 尝试删除分类 → 应抛异常
- [ ] 场景：创建菜品（含口味列表）→ 分页查询 → 修改口味 → 再查验证
- [ ] 场景：批量删除菜品 → 验证数据库实际删除条数

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化
- [ ] 配合 M8 安全测试，检查菜品名称/描述是否存在 XSS 漏洞

---

### M4 — 套餐管理 + 文件上传

**负责文件**：`SetmealController.java`、`CommonController.java`、`SetmealServiceImpl.java`、`SetmealMapper.java`、`SetmealDishMapper.java`

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测套餐 CRUD + 文件上传（6 个接口）
- [ ] Service 层：Mock Mapper，重点测起售套餐时菜品状态校验 → SetmealEnableFailedException
- [ ] Mapper 层：@MybatisTest + H2，验证套餐-菜品关联插入/删除
- [ ] 用判定表 + 等价类分别设计用例，W3 做方法对比

#### 集成测试阶段（W3）
- [ ] 场景：创建套餐（关联多个菜品）→ 停售其中一个菜品 → 尝试起售套餐 → 应抛异常
- [ ] 场景：文件上传（用 MockMultipartFile）→ 验证返回文件路径
- [ ] 场景：修改套餐的菜品关联 → 验证旧关联删除、新关联插入

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化
- [ ] 配合 M8 性能测试，关注文件上传接口的响应时间

---

### M5 — 管理端订单 + 定时任务

**负责文件**：`OrderController.java`(admin)、`OrderServiceImpl.java`(admin部分)、`OrderMapper.java`、`OrderTask.java`、`WebSocketTask.java`

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测订单条件查询、详情、接单/拒单/取消/派送/完成（7 个接口）
- [ ] Service 层：Mock Mapper，重点测非法状态转换 → 如已完成状态下再派送应被拦截
- [ ] Mapper 层：@MybatisTest + H2，验证多条件动态查询 SQL
- [ ] 用状态迁移图 + 场景法分别设计用例，W3 做方法对比

#### 集成测试阶段（W3）
- [ ] 场景：下单 → 接单 → 派送 → 完成，走完整状态链，验证每一步状态变更
- [ ] 场景：下单 → 拒单 → 验证订单状态和拒单原因入库
- [ ] 场景：模拟超时订单 → 触发 OrderTask → 验证自动取消

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化
- [ ] 配合 M8 性能测试，重点关注订单查询接口在高并发下的表现

---

### M6 — C端：用户 + 地址簿

**负责文件**：`UserController.java`(user)、`AddressBookController.java`(user)、`ShopController.java`(user)、`UserServiceImpl.java`、`AddressBookServiceImpl.java`

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测微信登录、地址 CRUD、店铺状态查询（8 个接口）
- [ ] Service 层：Mock Mapper，重点测首次登录自动注册、默认地址切换逻辑
- [ ] Mapper 层：@MybatisTest + H2，验证用户查询、地址默认标记更新
- [ ] 用等价类 + 边界值分别设计用例，W3 做方法对比

#### 集成测试阶段（W3）
- [ ] 场景：C端登录 → 拿到 Token → 新增地址 → 设默认 → 新增第二个地址 → 设默认 → 验证旧默认被取消
- [ ] 场景：查询店铺状态 → 验证 Redis 缓存与数据库一致
- [ ] 场景：C端 Token 访问 admin 接口 → 验证 JwtTokenUserInterceptor 拦截

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化
- [ ] 配合 M8 安全测试：验证用户 A 无法查看/修改用户 B 的地址

---

### M7 — C端：购物车 + 下单 + 支付（最复杂）

**负责文件**：`ShoppingCartController.java`、`OrderController.java`(user)、`PayNotifyController.java`、以及 C端浏览接口（Category/Dish/Setmeal user版）

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测购物车增删改查、下单、支付、支付回调（11 个接口）
- [ ] Service 层：Mock Mapper，重点测空购物车拦截、金额校验、下单事务回滚
- [ ] Mapper 层：@MybatisTest + H2，验证购物车数据隔离、订单明细批量插入
- [ ] 用场景法 + 正交实验分别设计用例，W3 做方法对比

#### 集成测试阶段（W3）⭐ 重点
- [ ] 场景：浏览菜品 → 加购 → 修改数量 → 清空 → 重新加购 → 提交订单 → 验证订单入库
- [ ] 场景：支付回调（Mock WeChatPayUtil）→ 验证订单状态变为已支付
- [ ] 场景：不同用户购物车数据隔离验证
- [ ] 场景：重复下单幂等性验证
- [ ] 场景：下单时库存不足的事务回滚验证

#### 系统测试阶段（W4）
- [ ] 配合 M1 跑 Postman 自动化（下单闭环是重点验证场景）
- [ ] 配合 M8 性能测试：订单提交接口的并发压测
- [ ] 配合 M8 安全测试：支付回调签名验证绕过、越权查看他人订单

---

### M8 — 报表 + 基础设施 + 性能 + 安全

**负责文件**：`ReportController.java`、`WorkSpaceController.java`、`GlobalExceptionHandler.java`、`AutoFillAspect.java`、JMeter 脚本

#### 单元测试阶段（W2）
- [ ] Controller 层：MockMvc 测营业额/用户/订单统计、Top10、Excel 导出、工作台概览（6+ 接口）
- [ ] 基础设施：逐一触发所有 BaseException 子类，验证 GlobalExceptionHandler 返回正确的错误码和消息
- [ ] 基础设施：验证 AutoFillAspect 在新增/修改时自动填充时间和用户 ID

#### 集成测试阶段（W3）
- [ ] 场景：创建订单 → 查询营业额统计 → 验证金额正确
- [ ] 场景：注册用户 → 查询用户统计 → 验证新增数正确
- [ ] 场景：Excel 导出 → 验证文件内容和列头
- [ ] 场景：WebSocket 连接 → 下单 → 验证管理端收到实时推送

#### 系统测试阶段（W4）⭐ 主力
- [ ] **性能测试**：用 JMeter 对 5 个高频接口分别压测 50/100/200/500 并发
- [ ] 记录 TPS、平均响应时间、P99 响应时间、错误率
- [ ] 绘制 TPS-并发曲线图，分析系统瓶颈
- [ ] **安全测试**：SQL 注入、XSS、越权访问、JWT 伪造、敏感信息泄露
- [ ] 整理性能数据和安全性结论，写报告第 3.2 节和第 6 章

---

## 环境说明

| 测试层级 | 环境 | 谁来跑 |
|---|---|---|
| 单元测试 | 本地 JDK17 + Maven + H2 内存库 | 每人本地 `mvn test` |
| 集成测试 | Docker MySQL + Redis（M1 维护） | 每人本地连接 Docker 或共享测试机 |
| API 自动化 | 共享测试机（M1 维护） | M1 执行 Postman Collection |
| 性能压测 | 共享测试机（M1 维护） | M8 执行 JMeter |

---

## 报告章节归属

| 报告章节 | 负责人 |
|---|---|
| 1. 范围 | M1 |
| 2. 测试计划 | M1 |
| 3.1 功能测试概述 | M1（汇总） |
| 3.2 性能测试概述 | M8 |
| 4. 单元测试 | M2~M8 各写自己模块 |
| 5. 集成测试 | M1 |
| 6. 系统测试 | M1 + M8 |
| 7. 测试总结（Jira） | M1 |
| 8. 社会可持续发展 | 全员各写 1-2 段，M1 整合 |
| 9. 个人学习总结 | 全员各写 300-500 字，M1 整合 |
